apiVersion: v1
kind: Service
metadata:
  name: catalogue
  namespace: roboshop
spec:
  selector:
    project: roboshop
    component: catalogue
    tier: db
  ports:
  - protocol: TCP
    port: 8080
    targetPort: 8080
---

apiVersion: v1
kind: Service
metadata:
  name: catalogue
  namespace: roboshop
spec:
  ClusterIp: None
  selector:
    project: roboshop
    component: catalogue
    tier: db
  ports:
  - protocol: TCP
    port: 8080
    targetPort: 8080